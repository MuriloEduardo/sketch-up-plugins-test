{
  "version": "0.2.0",
  "configurations": [
    {
      // Debug gem (ruby/debug) embarcado no SketchUp 2024+, via DAP.
      // EXPERIMENTAL / não configurado: depuração remota exige túnel SSH da
      // porta 7150 do desktop e mapeamento de caminhos (backlog técnico,
      // docs/workflow.md). Bootstrap: tools/debug/su_debug_bootstrap.rb.
      "name": "Attach to SketchUp",
      "type": "ruby_lsp",
      "request": "attach",
      "debugHost": "127.0.0.1",
      "debugPort": 7150,
      "localfs": true
    }
  ]
}
